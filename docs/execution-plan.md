# 分阶段执行计划

## 阶段总览

| 步骤 | 内容 | 预计产出 | 依赖 |
|------|------|----------|------|
| 0 | 建立项目文档体系 | 5 份文档 + CLAUDE.md | — |
| 1 | 搭建项目骨架 | 目录结构 + main.py 可运行 | 0 |
| 2 | DOCX 读取模块 | engine/reader.py | 1 |
| 3 | 匹配引擎 | engine/matcher.py | 2 |
| 4 | DOCX 生成模块 | engine/writer.py | 3 |
| 5 | GUI 主窗口 | gui/main_window.py | 4 |
| 6 | 手动映射弹窗 | gui/mapping_dialog.py | 5 |
| 7 | 集成测试 & 打包 | 可运行 .exe (19MB) | 6 |

> **状态：全部完成 ✓** (2026-05-24)
> 
> 注：第 6 步手动映射弹窗为占位实现，第 7 步打包需在桌面环境实测验证。

---

## 第 0 步：建立项目文档体系 ✓ 当前

### 任务清单
- [x] 创建 `docs/` 目录
- [x] 编写 `docs/requirements.md`
- [x] 编写 `docs/tech-spec.md`
- [x] 编写 `docs/design-spec.md`
- [x] 编写 `docs/execution-plan.md`（本文档）
- [x] 创建 `dev-logs/` 目录
- [x] 编写 `CLAUDE.md`

### 验收标准
- 5 份文档内容完整、格式规范
- CLAUDE.md 正确指引所有文件路径

---

## 第 1 步：搭建项目骨架

### 任务清单
- [x] 创建 `engine/`、`gui/` 目录及各目录下 `__init__.py`
- [x] 编写 `requirements.txt`
- [x] 安装依赖 `pip install -r requirements.txt`
- [x] 编写 `main.py` 入口（最小可运行骨架：显示空窗口）

### 验收标准
- `python main.py` 能启动一个空窗口
- 窗口标题显示"DOCX 格式统一工具"
- 窗口关闭正常退出

---

## 第 2 步：实现 DOCX 读取模块

### 任务清单
- [x] 编写 `engine/reader.py`
- [x] 实现段落读取（文本 + 格式）
- [x] 实现表格读取（结构 + 单元格文本）
- [x] 编写单元测试脚本验证读取正确性

### 验收标准
- 读取 `Reference Template.docx`，正确打印出 4 个段落 + 1 个 14×7 表格
- 读取 `Reference Input.docx`，正确打印出 2 个段落 + 1 个 15×4 表格
- 段落信息包含：文本、字体、字号、加粗、对齐
- 表格信息包含：行列数、每个单元格的文本

---

## 第 3 步：实现匹配引擎

### 任务清单
- [x] 编写 `engine/matcher.py`
- [x] 实现标签提取函数
- [x] 实现段落匹配（标签优先 + 顺序兜底）
- [x] 实现表格单元格匹配
- [x] 输出匹配结果映射表

### 验收标准
- 用参考文件测试：匹配结果能正确将输入文件中的"申报类别"对应到模板的"申报类别"
- 表格中相同字段名的单元格能正确匹配
- 未匹配项被正确标记

---

## 第 4 步：实现 DOCX 生成模块

### 任务清单
- [x] 编写 `engine/writer.py`
- [x] 实现"以模板为骨架复制"的核心逻辑
- [x] 实现段落文字替换
- [x] 实现表格单元格文字替换
- [x] 处理多余内容的附加

### 验收标准
- 用参考文件生成输出文件
- 输出文件的表格列数与模板一致（7 列）
- 输出文件的字体字号与模板一致
- 输出文件的文本内容来自输入文件
- 用命令行手动验证生成结果

---

## 第 5 步：实现 GUI 主窗口

### 任务清单
- [x] 编写 `gui/main_window.py`
- [x] 实现文件选择区域（3 个文件选择行）
- [x] 实现状态/日志显示区域
- [x] 实现"开始转换"按钮逻辑
- [x] 实现文件校验（文件存在、格式正确）
- [x] 实现错误提示对话框

### 验收标准
- 窗口显示正常，布局与设计规范一致
- 点击"浏览"能弹出系统文件选择框
- 选择文件后路径显示在输入框中
- 点击"开始转换"能完成完整流程并生成输出文件
- 日志区域正确显示处理状态

---

## 第 6 步：实现手动映射弹窗

### 任务清单
- [x] 编写 `gui/mapping_dialog.py`
- [x] 实现映射关系显示（模板列 ↔ 输入列）
- [x] 实现下拉选择修改映射
- [x] 实现"自动匹配"和"重置"按钮
- [x] 实现确认/取消逻辑

### 验收标准
- 弹窗正确显示模板列与输入列的映射关系
- 可以修改映射并生效
- "自动匹配"能恢复默认映射

---

## 第 7 步：集成测试 & 打包

### 任务清单
- [x] 用参考文件做端到端测试
- [x] 验证输出文件的格式一致性（段落数、表格行列数、字体、对齐）
- [x] 编写 `build.bat` 一键打包脚本
- [x] 编写 `DOCX_Format_Unifier.spec` — PyInstaller 配置文件
- [x] 添加 customtkinter 资源文件到打包（主题 JSON、字体 OTF/TTF、图标 ICO）
- [x] 补全隐藏依赖（darkdetect, PIL, tkinter.*, engine.*, gui.*）
- [x] 修复 `pyi_rth_pkgres.py` 运行时钩子崩溃（添加 try/except）
- [x] 构建双版本：`DOCX_Format_Unifier.exe`（正式版）+ `DOCX_Format_Unifier_console.exe`（调试版）

### 验收标准
- 参考文件转换结果正确
- 输出文件用 Word 打开格式与模板一致
- 打包后的 exe 可在无 Python 环境的 Windows 上运行 — **待桌面环境实测**
- 处理异常输入时不崩溃，有友好提示 — **需 GUI 实测**

---

*本文件将在开发过程中持续更新。*
